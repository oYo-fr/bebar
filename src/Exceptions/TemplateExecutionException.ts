import {BebarException} from './BebarException';

/**
 * Exception that may occurs when processing a template with its
 *  data has failed.
 */
export class TemplateExecutionException extends BebarException {
};
